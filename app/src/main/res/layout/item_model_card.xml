<?xml version="1.0" encoding="utf-8"?>
<!--
    Single card in the model library grid.
    Place this file at: res/layout/item_model_card.xml
-->
<androidx.cardview.widget.CardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_margin="4dp"
    app:cardCornerRadius="14dp"
    app:cardBackgroundColor="@color/card_background"
    app:cardElevation="0dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical">

        <!-- Thumbnail with size badge overlay -->
        <FrameLayout
            android:layout_width="match_parent"
            android:layout_height="110dp"
            android:background="@color/card_image_background">

            <ImageView
                android:id="@+id/imgModelThumbnail"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:scaleType="centerCrop"
                android:contentDescription="@string/model_thumbnail" />

            <!-- File size overlay badge -->
            <TextView
                android:id="@+id/tvFileSize"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_gravity="bottom|end"
                android:layout_margin="8dp"
                android:paddingStart="6dp"
                android:paddingEnd="6dp"
                android:paddingTop="2dp"
                android:paddingBottom="2dp"
                android:background="@drawable/bg_size_badge"
                android:textColor="#FFFFFF"
                android:textSize="10sp" />

        </FrameLayout>

        <!-- Card body -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:paddingStart="10dp"
            android:paddingEnd="10dp"
            android:paddingTop="9dp"
            android:paddingBottom="10dp">

            <!-- Model name -->
            <TextView
                android:id="@+id/tvModelName"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:textColor="@color/text_primary"
                android:textSize="13sp"
                android:textStyle="bold"
                android:singleLine="true"
                android:ellipsize="end"
                android:letterSpacing="-0.01" />

            <!-- Dimensions -->
            <TextView
                android:id="@+id/tvDimensions"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="2dp"
                android:textColor="@color/text_secondary"
                android:textSize="11sp"/>

            <!-- Edit date -->
            <TextView
                android:id="@+id/tvEditDate"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:textColor="@color/text_secondary"
                android:textSize="11sp"/>

            <!-- Badge row -->
            <LinearLayout
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:orientation="horizontal"
                android:layout_marginTop="7dp"
                android:gravity="center_vertical">

                <!-- "✓ Saved" profile badge -->
                <TextView
                    android:id="@+id/tvSavedBadge"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="@string/saved_badge"
                    android:textSize="10sp"
                    android:textStyle="bold"
                    android:paddingStart="7dp"
                    android:paddingEnd="7dp"
                    android:paddingTop="2dp"
                    android:paddingBottom="2dp"
                    android:background="@drawable/bg_badge_saved"
                    android:textColor="@color/badge_saved_text"
                    android:visibility="gone" />

                <Space
                    android:layout_width="5dp"
                    android:layout_height="1dp" />

                <!-- "Imported" badge -->
                <TextView
                    android:id="@+id/tvImportedBadge"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="@string/imported_badge"
                    android:textSize="10sp"
                    android:textStyle="bold"
                    android:paddingStart="7dp"
                    android:paddingEnd="7dp"
                    android:paddingTop="2dp"
                    android:paddingBottom="2dp"
                    android:background="@drawable/bg_badge_imported"
                    android:textColor="@color/badge_imported_text"
                    android:visibility="gone" />

            </LinearLayout>

        </LinearLayout>

    </LinearLayout>

    <!-- Three-dots overflow — anchored to top-end corner -->
    <ImageButton
        android:id="@+id/btnModelOptions"
        android:layout_width="32dp"
        android:layout_height="32dp"
        android:layout_gravity="top|end"
        android:layout_margin="6dp"
        android:background="@drawable/bg_icon_btn_overlay"
        android:src="@drawable/ic_more_vert"
        android:contentDescription="@string/more_options"
        android:scaleType="centerInside"
        android:padding="4dp" />

</androidx.cardview.widget.CardView>